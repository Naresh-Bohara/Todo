<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TodoApp - Home</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">  
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        h1 {
            font-size: 40px;
            color: #2c3e50;
            font-weight: 600;
            margin-bottom: 50px;
        }


        .section {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            max-width: 650px;
            width: 100%;
            text-align: center;
        }


        .btn-container {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 40px;
        }

        .btn {
            padding: 16px 40px;
            font-size: 18px;
            font-weight: 500;
            text-decoration: none;
            border-radius: 30px;
            color: white;
            display: inline-block;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            border: none;
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.1);
        }


        .btn-primary {
            background-image: linear-gradient(135deg, #3498db, #8e44ad);
            border: 2px solid #3498db;
        }

        .btn-primary:hover {
            background-image: linear-gradient(135deg, #2980b9, #9b59b6);
            transform: translateY(-4px);
            box-shadow: 0 12px 18px rgba(0, 0, 0, 0.15);
        }

  
        .btn-secondary {
            background-image: linear-gradient(135deg, #ff7e5f, #feb47b);
            border: 2px solid #ff7e5f;
        }

        .btn-secondary:hover {
            background-image: linear-gradient(135deg, #ff6a3d, #e76f51);
            transform: translateY(-4px);
            box-shadow: 0 12px 18px rgba(0, 0, 0, 0.15);
        }

        @media (max-width: 600px) {
            .btn {
                font-size: 16px;
                padding: 14px 30px;
            }
            h1 {
                font-size: 30px;
            }
        }
    </style>
</head>
<body>
    <div class="section">
        <h1>Welcome to TodoApp!</h1>

        <div class="btn-container">
            <a href="/api/v1/todos" class="btn btn-primary">Show All Todos</a>
            <a href="/api/v1/todos/new" class="btn btn-secondary">Add New Todo</a>
        </div>
    </div>

</body>
</html>
